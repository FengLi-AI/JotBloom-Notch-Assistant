These screenshots show the native Mac 1.0.4 interface in dark and light appearances.
Captured from an isolated local store on 2026-09-15. Notes, prompts, clipboard entries and chat messages are synthetic demonstration data; chat replies are fixed examples, not live model evaluation results.
The Windows client has a separate native WPF implementation and is validated separately in the Windows UI workflow.
