# 开源客户端工作约定

- 本目录是 `FengLi-AI/JotBloom-Notch-Assistant` 的公开仓库，基础功能持续免费、MIT 开源。
- 灵感库、剪贴板、提示词、AI 对话、搜索、基础设置与其修复在本仓库开发，必须能独立构建使用，不依赖私有仓库、登录或激活服务。
- 桌宠、刘海拖入灵感、付费动效、专属角色素材、授权服务实现放在独立私有仓库；不要复制到本仓库，不要把完整商业客户端仅用一个开关隐藏后公开源码。
- 通用扩展接口可以公开，具体付费实现不可公开。底层只发通用事件，不能反向依赖商业模块。
- 官网 `site/` 和已发布历史保留。未经明确发布要求，不改下载包、版本号、Pages 分支或自动发布。
- 当前 Mac 优先；Windows 原有源码保留，暂缓新增功能开发。
- 提交前运行 `python3 scripts/check-repository-boundary.py`。本地可用 `git config core.hooksPath scripts/git-hooks` 启用推送前检查；同时审查提交 diff，自动检查不能识别所有商业代码。
- 只按任务范围提交明确文件；勿导入历史工作区、用户数据、运行日志、API Key、签名证书或私有构建目录。
