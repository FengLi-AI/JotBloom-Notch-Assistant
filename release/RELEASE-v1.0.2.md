# 萌生 v1.0.2 · 关于与版本检查

应用版本 **1.0.2**，构建 **1104**。Apple Silicon / Intel 双架构安装包：`JotBloom-1.0.2-universal.dmg`，SHA-256 见附件 `SHA256SUMS.txt`。

安装包 5,553,885 字节。SHA-256：`f2571b8de70d7664fc6fddfb966028977765c7388b5c561290490a812b627bd3`。

## 更新内容

- **快捷操作置顶**：在“设置 → 通用”的最上方查看呼出、展开、搜索与退出快捷操作。
- **新增关于**：展示萌生 · JotBloom、官网标语“给闪过的想法，一点空间。”、作者李烽立以及邮箱、微信、小红书主页和抖音号，不展示电话号码；附 Bug / 建议联系提示。
- **版本与检查更新**：展示实际应用版本和构建号；点击“检查最新版本”查询 GitHub，发现新版本时可打开对应发布页面。网络失败会提示重试，也可直接打开版本页面。

只在主动检查时连接 GitHub，不上传本地内容或密钥；不会自动下载、安装或替换应用。检查只考虑已发布且有可用 Mac 安装包的正式版本，排除草稿、预发布与仅 Windows 的包。

此前公开标签为 `v1.0.0-preview.3`，本次统一版本编号为 `v1.0.2`，保留旧版本发布记录。首次存储位置选择和快捷键录制修正继续保留。

## 安装与验证边界

先保存内容并退出旧版，保留重要数据和旧 App 备份，再打开 DMG，将 **萌生｜JotBloom.app** 拖入“应用程序”。卸载 App 本身不会清除原数据，升级继续使用原保存位置。应用标识、SQLite V7 和钥匙串服务标识保持一致。

本版本使用 ad-hoc 签名，**尚未经过 Apple 公证**。请核对来源和 SHA-256，并按系统指引安装；不需要关闭系统安全保护。构建目标 macOS 13 及以上，最低系统、Intel 真机和完整安装/升级权限体验仍待实机验证。

本次 363 项 XCTest、64 项隔离 AppKit 检查通过，包含版本选择/错误响应、现有核心回归、7 个设置页截图及快捷键/焦点回归。构建和合成事件不替代用户实际安装、键盘与网络环境验收。Windows 客户端现已独立发布，可从[官网](https://fengli-ai.github.io/JotBloom-Notch-Assistant/)选择 Windows 版下载。
